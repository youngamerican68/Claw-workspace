export const metadata = {
  title: 'Project Dashboard',
  description: 'Track all AI opportunity projects',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
